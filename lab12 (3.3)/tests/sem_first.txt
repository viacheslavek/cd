enum ScreenType1 {
  SCREEN_TYPE_TEXT1
} screen_type1;

enum Color {
  COLOR_RED = 1,
  COLOR_GREEN = 2,
  COLOR_BLUE = 2*2,
  COLOR_HIGHLIGHT = 8,
};

enum ScreenType4 {
  SCREEN_TYPE_TEXT,
  SCREEN_TYPE_GRAPHIC,
  BUFFER_SIZE
} screen_type;

enum {
  SCREEN_TYPE_TEXT2
} screen_type2;

enum {
  HPIXELS = 480,
  WPIXELS = 640,
  HCHARS = 24,
  WCHARS = 80,
};

enum {
  PAGE_SIZE = 4096,
  PAGES_FOR_BUFFER = (BUFFER_SIZE + PAGE_SIZE - 1) / PAGE_SIZE
};

enum ScreenType3 screen_type3;
